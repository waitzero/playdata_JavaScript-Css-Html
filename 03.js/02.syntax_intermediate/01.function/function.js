//기본적인 함수 정의,
//함수 작성 방식 첫 번째, 함수 선언문 방식

//함수키워드 함수명(parameter 파라미터)
function add(a, b) {
  //두 수의 덧셈 함수
  const result = a + b;
  //   return a + b;
  console.log(result);
}

//함수호출(call)
add(2, 3);
